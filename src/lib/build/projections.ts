import { Context } from '../../types';
import { Log } from '../../util';
import { SubjektModel } from 'subjekt';

export const applyProjections = (context: Context, model: SubjektModel): SubjektModel[] => {
    return [];
}
