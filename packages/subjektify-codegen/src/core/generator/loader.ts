import { SubjektifyRuntimeEnvironment } from "subjektify";

export class CodeGeneratorLoader {
    static load(sre: SubjektifyRuntimeEnvironment) {
        const configs = sre.config.codegen || [];
        
    }
}
