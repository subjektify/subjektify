import { SubjektModel } from "subjekt";
import { Projection } from "./type-extensions";

export const applyProjections = async (model: SubjektModel, projections: Projection[]): Promise<SubjektModel[]> => {
    return [];
}
